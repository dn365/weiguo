<div id="navbar" class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <%= link_to raw("#{image_tag("logo.png")}微果"), root_url, class:"navbar-brand" %>
  </div>
  <div class="collapse navbar-collapse">
    <ul class="nav navbar-nav">
      <!-- <li style="display: none;"><a href="#home"></a></li> -->
      <li class="<%= "active" if @site_index %>">
        <%= link_to raw("<i class='icon-leaf'></i>单品鲜果<span class='arrow-left'></span>"), root_url %>
      </li>
      <li>
        <%= link_to raw("<i class='icon-gift'></i>热销组合<span class='arrow-left'></span>"), "#", class:"scroll" %>
      </li>
      <li>
        <%= link_to raw("<i class='icon-magnet'></i>手动大拼盘<span class='arrow-left'></span>"), "#", class:"scroll" %>
      </li>
      <% if user_signed_in? %>
      <li class="<%= "active" if @user_show %>">
        <%= link_to raw("<i class='icon-user'></i>#{current_user.email.split("@")[0]}<span class='arrow-left'></span>"), users_url %>
      </li>
      <li>
        <%= link_to raw("<i class='icon-off'></i>注销登录<span class='arrow-left'></span>"), destroy_user_session_path, :method => :delete %>
      </li>
      <% else %>
      <li>
        <%= link_to raw("<i class='icon-dollar'></i>用户登录<span class='arrow-left'></span>"), new_user_session_url %>
      </li>
      <% end %>
      <li>
        <%= link_to raw("<i class='icon-align-left'></i>企业简介<span class='arrow-left'></span>"), "#", class:"scroll" %>
      </li>
      <li>
        <%= link_to raw("<i class='icon-comment'></i>其他信息<span class='arrow-left'></span>"), "#", class:"scroll" %>
      </li>
    </ul>
  </div><!--/.nav-collapse -->
</div>